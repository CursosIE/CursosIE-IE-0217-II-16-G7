Disculple, pero el profesor no nos habia dado los permisos hasta el martes en la clase,
sin embargo ahi puede ver que el archivo que enviamos a tiempo en el schoology es el mismo
que subimos aca.

src/ contiene el codigo en c++ y el makefile correspondiente. Ademas el error que tiraba lo pudimos
resolver en la clase del martes y era algo muy pequenno, sin embargo subimos el archivo que quedo listo el domingo.
